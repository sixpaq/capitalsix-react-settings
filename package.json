{
  "name": "capitalsix-react-settings",
  "version": "0.0.2",
  "description": "Load settings from server side",
  "main": "dist/index.js",
  "devDependencies": {
    "@babel/cli": "^7.17.10",
    "@babel/core": "^7.17.10",
    "@babel/polyfill": "^7.12.1",
    "@babel/preset-env": "^7.12.1",
    "@babel/preset-react": "^7.12.5"
  },
  "scripts": {
    "build": "rm -rf dist && NODE_ENV=production babel src --out-dir dist --copy-files"
  },
  "keywords": ["react", "settings", "server-side", "context", "provider", "ci/cd", "deployment"],
  "author": "Mike Wijnen",
  "license": "ISC",
  "dependencies": {
  }
}
